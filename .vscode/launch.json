{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run TSPerf Type Lens",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/test-fixtures/complex-types.ts"
      ]
    }
  ]
}
